import { AppButton } from "@/components/app-button";

export const FinishPurchaseButton = () => {
  return (
    <div className="mt-9">
      <AppButton>
        Finalizar compra
      </AppButton>
    </div>
  );
}